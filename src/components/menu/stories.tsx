import type { Meta, StoryObj } from '@storybook/react';

import Menu from '.';

const meta: Meta = {
  title: 'Menu',
  component: Menu,
  argTypes: {},
};

export default meta;

export const Default: StoryObj = {};
